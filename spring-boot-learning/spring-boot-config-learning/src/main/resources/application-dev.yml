server:
  port: 8082


management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

spring:
  application:
    name: config
  boot:
    admin:
      client:
        url: http://localhost:8099